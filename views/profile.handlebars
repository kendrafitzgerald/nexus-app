<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title>User Dashboard</title>

<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">User Dashboard</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link" href="/logout">Logout</a>
        </li>
      </ul>
    </div>
  </nav>
  <div class="container">
    <h1 class="text-center my-4">Welcome {{user.username}}!</h1>
    <div class="row">
      <div class="col-md-4">
        <div class="card">
          <div class="card-header">Profile</div>
          <ul class="list-group list-group-flush">
            <li class="list-group-item"><strong>Name:</strong> {{user.username}}</li>
            <li class="list-group-item"><strong>Email:</strong> {{user.email}}</li>
            <li class="list-group-item"><strong>School:</strong> {{user.school}}</li>
            {{#if user.major}}
            <li class="list-group-item"><strong>Major:</strong> {{user.major}}</li>
            {{/if}}
            {{#if user.clubs}}
            <li class="list-group-item"><strong>Clubs:</strong> {{user.clubs}}</li>
            {{/if}}
            {{#if user.start_date}}
            <li class="list-group-item"><strong>Start Date:</strong> {{format_date user.start_date}}</li>
            {{/if}}
            {{#if user.graduation_date}}
            <li class="list-group-item"><strong>Graduation Date:</strong> {{format_date user.graduation_date}}</li>
            {{/if}}
          </ul>
        </div>
      </div>


      {{!-- we need user name, that user's school information, users posts, option to
      create a new post, and this is where spotify will go? --}}
      {{#if blogposts.length}}
      {{#each blogposts}}
      <div class="card" style="width: 18rem;">
        <img src="..." class="card-img-top" alt="...">
        <div class="card-body">
          <h5 class="card-title">{{title}}</h5>
          <p class="card-text">{{content}}</p>
        </div>
        <ul class="list-group list-group-flush">
          <li class="list-group-item">You posted this on: {{format_date createdAt}}</li>
        </ul>
      </div>
      {{/each}}
      {{else}}
      <p>You have no posts! Create one now.</p>
      {{/if}}

      <div class="row mt-4" id="new-post-form" style="display:none">
        <div class="col-md-6">
          <h3>Create a new blog post</h3>

          <form class="form new-blogpost-form">
            <div class="form-group">
              <label for="blogpost-title">Title:</label>
              <input class="form-input" type="text" id="blog-title" name="blogpost-title" />
            </div>
            <div class="form-group">
              <label for="content">Content:</label>
              <textarea class="form-input" id="content" name="content"></textarea>
            </div>
            <div class="form-group">
              <button type="submit" class="btn btn-primary" id="submit-button">Submit</button>
            </div>
          </form>
        </div>
      </div>

      <button class="btn btn-primary" id="create-button">Create a New Post</button>

      <script src="/js/createPost.js"></script>